// Types


// There are two main types in javascript

// Primitive types and the Complex Types

// primitive(numbers, strings, undefined, null, boolean) => work directly on the value
// complex (objects, arrays, functions) => work on its reference

// Number
const age = 23;
const meanMark = 49.05;

// string
let name = "Mike";
let schoolName = 'Oxford';

// undefined
const state;

// null
let myValue = null;

// boolean
const isLoading = true;
const isPaid = false; 


// Object

const vehicle={
    color: 'white',
    size: '24mgb',
    isFull: false
}

// Array

const marks=[23, 45.5, 60, 80];

// Function

// simple function
function sayHello(){
    console.log(hello);
}

// arrow function
const getTotalMarks = () =>{
    console.log('Fail');
}


